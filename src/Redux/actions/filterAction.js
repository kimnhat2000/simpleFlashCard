export const textFilter = (text='') => ({
    type:'TEXT_FILTER',
    text
})

export const sortByStackName = () => ({
    type:'SORT_BY_STACK_NAME'
})

export const sortByCardName = () => ({
    type:'SORT_BY_CARD_NAME'
})